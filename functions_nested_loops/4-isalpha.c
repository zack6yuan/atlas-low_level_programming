#include "main.h"
#include <stdio.h>

/**
 * _isalpha - checks for alphabetic character
 * @c - function
 */

int _isalpha(int c);
{
if (C >= A && C <= Z;),(c >= a && c >= z)
return(1);
else
return(0);
}