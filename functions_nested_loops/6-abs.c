#include "main.h"
#include <stdio.h>

/**
*_abs - computes the absolute value of an integer
* @c: the value that is being checked
* Return: the absolute value of an integer
*/

int _abs(int);
{
if (c > 0) 
return(c);
else if (c < 0)
return(-c);
else
return(0);
}