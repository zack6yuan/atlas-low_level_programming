#include "main.h"
#include <stdio.h>

/**
 * rev_string - reverses a string
 * @s: the string
 * Return: void 
 */

void rev_string(char *s)
{
int x;
int length = 0;

for (x = 0; s[x] != '\0'; x++)
length++;
}