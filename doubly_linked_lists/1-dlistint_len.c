#include "lists.h"
/**
 * print_dlistint - prints the number of elements of a dlistint_t list
 * @h: the head of the list
 * Return: the number of elements
*/
size_t dlistint_len(const dlistint_t *h)
{
        
}