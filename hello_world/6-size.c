#include <stdio.h>

/**
 * main - prints the text required
 * 
 * Return: 0
*/
 int main(void)
 {
    printf("the size of a char is:", sizeof(char));
    printf("the size of an int it:", sizeof(int));
    printf("the size of a long int:", sizeof(long int));
    printf("the size of a float:", sizeof(float));

    return (0);
 }
