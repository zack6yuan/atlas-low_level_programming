#include "hash_tables.h"
/**
 * hash_table_print - prints a hash table
 * @ht: the hash table, if it is NULL, don't print anything
 * Return: the hash table
 */
void hash_table_print(const hash_table_t *ht)
{
        
}